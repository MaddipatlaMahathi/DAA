def average_even_divisible_by_three(nums):
    total = 0
    count = 0
    for num in nums:
        if num % 6 == 0:
            total += num
            count += 1
    return total // count if count > 0 else 0

nums = [1, 3, 6, 10, 12, 15]
print(average_even_divisible_by_three(nums))
